apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # URL pointing to the Prometheus service in Docker network
    # docker inspect -f "{{ .NetworkSettings.IPAddress }}" or localprometheus
    url: http://prometheus:9090
    isDefault: true
    editable: true