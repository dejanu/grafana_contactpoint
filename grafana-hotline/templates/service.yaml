apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-hotline.fullname" . }}
  labels:
    {{- include "grafana-hotline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-hotline.selectorLabels" . | nindent 4 }}
